<h1> captured lemurs</h1>
<!-- 
Filters don't access map data at this time.  Will be implemented in next version.  
Need to give option for "no value included"
Values selected from the drop downs (ideally checkboxes) will used to query the map to note those regions that meet the query criteria.-->

<div class="filter_options_container">
	<h2>dropdowns to filter data</h2>
</div>

<div class="form-section">
  <form class="filter_form">
		<%= select("lemur", "species", Lemur.all.collect(&:species).uniq.sort()) %>
    <input type="submit" value="Filter" />
  </form>
	<form class="filter_form">
		<%= select("lemur", "region", Lemur.all.collect(&:region).uniq.sort()) %>
    <input type="submit" value="Filter" />
  </form>
	<form class="filter_form">
		<%= select("lemur", "year", Lemur.all.collect(&:year).uniq.sort()) %>
    <input type="submit" value="Filter" />
  </form>
</div>

<div class="instructions">
	<!-- Filtering not in place yet.   -->
	
</div>


<div id="map"></div>


